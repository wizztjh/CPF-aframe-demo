{
  "name": "CPF-VRcollab",
  "description": "CPF",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "start": "budo src/index.js:dist/bundle.js --live --verbose --port 3000 --open -t babelify",
    "deploy": "browserify src/index.js -o dist/bundle.js && ghpages && rm dist/bundle.js",
    "ghpages": "browserify src/index.js -o dist/bundle.js && ghpages && rm dist/bundle.js"
  },
  "devDependencies": {
    "aframe": "^0.1.1",
    "aframe-core": "^0.1.3",
    "browserify-css": "^0.8.4",
    "budo": "^7.0.0",
    "ghpages": "0.0.3",
    "polymerize": "^1.0.0"
  },
  "keywords": [
    "aframe-example",
    "aframe-scene"
  ]
}
